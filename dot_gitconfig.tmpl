{{ if (or (eq .chezmoi.hostname "LAPTOP-QUENTIN") (eq .chezmoi.hostname "laptop-quentin")) }}
[user]
	email = contact@yovach.dev
	name = Quentin
{{ end }}

[init]
	defaultBranch = main
[core]
  editor = vim
  eol = lf
  autocrlf = false
